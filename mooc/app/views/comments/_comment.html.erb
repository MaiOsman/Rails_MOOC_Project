<div class="comment_content">
  <% @comments.each do |comment| %>
  <strong> <%= comment.try(:user).try(:name) %> </strong>

  <p> <%= comment.body %> </p>
  <p> <%= time_ago_in_words(comment.created_at) %> </p>
  <% end %>
</div>
